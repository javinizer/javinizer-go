package batch

const (
	errorResponseKey          = "error"
	messageResponseKey        = "message"
	applyGenerationKey        = "apply_generation"
	jobNotFoundMessage        = "Job not found"
	factoryUnavailableMessage = "batch job factory unavailable — workflow factory not ready; retry the request"
)
